extends ../layout

block content
  .container
    h1.main-title Users
    
    table.table.table-hover
      thead
        tr
          th Name
          th Email
          th Created At
          th
      tbody
        each user in users
          tr
            td
              a(href=`/admin/${user._id}`)= user.name
            td= user.email
            td= moment(user.createdAt).format('YYYY-MM-DD HH:mm:ss')
            td
              a.btn.btn-sm.btn-outline-secondary(href=`/admin/${user._id}/edit`) 
                | #[i.fa.fa-pencil] Edit
              | 
              a.btn.btn-sm.btn-outline-danger(href=`/admin/${user._id}?_method=DELETE`)
                | #[i.fa.fa-trash] Delete
    a.btn.btn-primary(href='/admin/create') #[i.fa.fa-plus] admin 계정 만들기
