extends ../layout

block content
  .container
    h1.main-title 개인정보 수정
    form(action=`/users/${currentUser._id}/edit?_method=PUT`, method='POST')
      .form-group
        label(for='name') 이름
        input.form-control(type='text', id='name', name='name', placeholder='이름을 입력하세요', value=user.name)
      .form-group
        label(for='email') 이메일
        input.form-control(type='email', id='email', name='email', placeholder='이메일을 입력하세요', value=user.email)
      .form-group
        label(for='current_password') 현재 비밀번호
        input.form-control(type='password', id='current_password', name='current_password', placeholder='현재 비밀번호를 입력하세요')
      .form-group
        label(for='password') 새 비밀번호
        input.form-control(type='password', id='password', name='password', placeholder='새 비밀번호를 입력하세요')
      .form-group
        label(for='password_confirmation') 새 비밀번호 확인
        input.form-control(type='password', id='password_confirmation', name='password_confirmation', placeholder='새 비밀번호를 한번 더 입력하세요')
      a.btn.btn-outline-secondary(href='javascript:window.history.back();') Back 
      | 
      button.btn.btn-primary(type='submit') 수정하기
    a.btn.btn-sm.btn-outline-danger(href=`/users/${user._id}?_method=DELETE`)
      | #[i.fa.fa-trash] Delete

